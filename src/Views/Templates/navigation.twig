<nav class="navbar top-bar navbar-inverse">
    <div class="navbar-header">
        <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
		</button>
        <a class="navbar-brand" href="{{ path_for('home') }}"><img src="{{ settings.logo }}" srcset="{{ settings.logo2x }} 2x"></a>
    </div>
    <div class="collapse navbar-collapse js-navbar-collapse">
        <ul class="nav navbar-nav">
            <li><a href="{{ path_for('home') }}">Home</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right ">
        {% if authentication.check %}
            <li class="dropdown">
                <a href="#" class="dropdown-toggle user" data-toggle="dropdown" role="button" aria-expanded="false">{{ authentication.user.name }} <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                    <li><a href="{{ path_for('authentication.password.change') }}">Change password</a></li>
                    <li class="divider"></li>
                    <li><a href="{{ path_for('authentication.signout') }}">Sign Out</a></li>
                </ul>
            </li>
        {% else %}
            <li><a href="{{ path_for('authentication.signup') }}">Sign up</a></li>
            <li><a href="{{ path_for('authentication.signin') }}">Sign in</a></li>
        {% endif %}
        </ul>
    </div>

</nav>
{% include 'Templates/sidebar.twig' %}
